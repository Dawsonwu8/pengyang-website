---
// 檔案路徑: src/pages/index.astro
import Layout from '../layouts/Layout.astro';
---

<Layout title="首頁">
	<div class="hero">
		<img src="/hero-image.png" alt="朋洋映像主視覺" class="hero-image" />
		<div class="hero-text">
			<h1>朋洋映像</h1>
			<p>光影敘事，成就品牌故事。朋洋映像整合行銷。</p>
		</div>
	</div>
</Layout>

<style>
	.hero {
		position: relative; /* 作為文字定位的基準 */
		height: 80vh; /* 佔螢幕 80% 的高度 */
		display: flex;
		justify-content: center;
		align-items: center;
		margin-top: -6rem; /* 讓圖片往上，與導覽列融合 */
	}
	.hero-image {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		object-fit: cover; /* 讓圖片填滿容器，不變形 */
		z-index: -1;
		opacity: 0.3; /* 調暗背景圖，讓文字更突出 */
	}
	.hero-text {
		text-align: center;
		padding: 0 20px;
	}
	.hero-text h1 {
		font-size: 4rem;
		color: white; /* 強制設為純白以求最高對比 */
		text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.8); /* 加上更深的文字陰影增加立體感 */
		margin-bottom: 1rem;
	}
	.hero-text p {
		font-size: 1.5rem;
		color: #e0e0e0; /* 使用高亮度的灰色 */
		text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.8);
		max-width: 600px;
	}
</style>